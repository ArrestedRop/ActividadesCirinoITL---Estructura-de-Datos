Algoritmo Secuencial

1. goal(): para capturar y buscar datos de personas almacenadas en una
         colección, usaremos captura de datos por centinela y busqueda
         secuencial en un arreglo de apuntadores a objetos

2.1 data(): Usaremos un arreglo de personas llamado grupo[] con 25 lugares.
         este arreglo representa la coleccion de objetos. Cada persona
         es un objeto que se modela como una clase encapsulada con atributos
         nombre, numero de lista, numero de control, fecha de nacimiento,
         peso y estatura.

         cuenta = 0
         Escribe("Deme el nombre: "); nombre = ?
         mientras (nombre != "fin") empezar
           Escribe("De # de control:"); control = ?
           Escribe("De Fecha de Nacimiento: ") ; fNac = ?
           Escribe("Peso de la persona: ") peso = ?
           Escribe("Estatura de la persona: "); talla = ?
           grupo[cuenta] = nueva Persona(nombre, control, fNac, peso, talla);
           cuenta = cuenta + 1
         terminar

 2.2 read(archi): Datos del archivo de entrada llamado archi
         cuenta = 0
         in = abrir(archi);
         mientras (!eof(in)) empezar
           pers = leerRegistro(in)
           grupo [cuenta] = nueva Persona();
           grupo [cuenta].setNombre(in.getNombre())
           grupo [cuenta].setControl(in.getControl())
           grupo [cuenta].setFNac(in.getFNac())
           grupo [cuenta].setPeso(in.getPeso())
           grupo [cuenta].setTalla(in.getTalla())
           cuenta = cuenta + 1
         terminar

2.3 entry(prompt): Escribe(prompt); cadena = ?

3. search(name, grupo, cuenta): regresa un entero
         compara = 0
         desde indice = 0 hasta cuenta - 1; incrementar indice
            empezar
               compara = compara + 1
               // Usamos equals() porque en Java == compara direcciones de memoria
               Si (name.equals(grupo[indice].getName())) empezar
                 retornar indice; // ¡Éxito! Retornamos la posición
               terminar
            terminar

         // Si salimos del ciclo sin retornar, es que no estaba
         retornar -1

3.1 searchBin(name, grupo, cuenta): regresa un entero
        compara = 0;
        desde indice

 4.1 listar():
        Escribe("Listado de personas en el arreglo ")
        Escribe("___________________________________________________________")
        Escribe("Num.  control   nombre            ")
        Escribe(" Fecha Nacimiento     Peso     Estatura")
        Escribe("___________________________________________________________")
        Desde i = 0 hasta i = cuenta empezar
           nombre = grupo[i].getNombre()
           control = grupo[i].getControl()
           fechaNac = grupo[i].getFechaNac()
           peso = grupo[i].getPeso()
           talla = grupo[i].getEstatura()
           Escribe (i + " " + control + " " + nombre + " ")
           Escribe (fechaNac + " " + peso + " " + talla)
        terminar

 4.2 outs(pos):
        Escribe("El nombre solicitado: " + name)
        if (encontrado) empezar
           Escribe ("esta en la posicion "+ pos)
           Escribe ("su numero de control es:" + grupo[pos].getControl())
           Escribe ("Nacio el :" + grupo[pos].getFNac())
           Escribe ("Su peso es: "+ grupo[pos].getPeso())
           Escribe ("Su talla es: "+ grupo[pos].getTalla())
        terminar
        de otro modo Escribe("no fue encontrado en el grupo")
        Escribe("se realizaron "+ compara + " preguntas para la busqueda")

5.0 sail():
       repetir empezar
         Escribir("opciones de busqueda:")
         Escribir("___________________________________________")
         Escribir("Usted puede:")
         Escribir("___________________________________________")
         Escribir("1. Introducir datos de personas por teclado")
         Escribir("2. Leer datos de un archivo de texto")
         Escribir("3. Desplegar la lista de personas")
         Escribir("4. Buscar alguna persona por nombre")
         Escribir("5. Salir del programa")
         Escribir("___________________________________________")
         Escribir("Su eleccion: ")
         op = ?
         segun (op) empezar
           caso 1: data(); romper
           caso 2: archi = pide("nombre del archivo a leer");
                   leer(archi); romper;
           caso 3: listar(); romper;
           caso 4: name = pide ("nombre de la persona a buscar")
                   pos = busqueda (name);
                   outs(pos)
         terminar
       terminar
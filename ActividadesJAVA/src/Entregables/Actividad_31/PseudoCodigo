/*
Tecnológico Nacional de México
Instituto Tecnológico de León
Ingeniería en Sistemas Computacionales
Estructuras de datos
Lunes y miercoles 7:00 a 8:45
viernes 7:00 a 7:50
Alumno: Villagomez Magaña Maximo Javier
Tarea 31
Fecha: 12/5/2026
*/

Variables Globales:
Long MS,PS,PRS,MB,PB,PRB
int[] Arreglo
int num

1. METODO goal():
    ESCRIBE "Script basico que permite medir el tiempo en cada una de los escenarios"
    ESCRIBE "de los algoritmos de busqueda secuencial y binaria"

2. METODO data(): //Generador datos
    MIENTRAS (isNum(num)) EMPEZAR
        ESCRIBE "Deme la cantidad de terminos que desea generar"
        CAPTURAR num
        int[] Arreglo1 = new Arreglo[num]
    TERMINAR
   REGRESAR num
   TERMINAR METODO

2.1 METODO generador(): //GENERADOR
       CREAR Int Arreglo1[num]

       DESDE i = 0 HASTA (Arreglo1.lenght()) EMPEZAR:
            Arreglo[i] = num+1
       TERMINAR
       COPEAR Arreglo1[] A Arreglo[]
    TERMINAR METODO

3. PROCESOS
3.1 METODO searchSecuencial(int Arreglo[], int busq):
        SI (Arreglo[] == null) REGRESAR -1;
        DESDE i = 0 HASTA(Arreglo.lenght()) EMPEZAR:
            SI (Arreglo[i] == busq) REGRESAR i;
        TERMINAR
    TERMINAR METODO

3.2 METODO searchBinaria(int Arreglo[], int busq):
       SI (Arreglo[] == null) REGRESAR -1;
        int inicio = 0
        int fin = (Arreglo.lenght() - 1)
        int mitad = 0

        MIENTRAS ( inicio <= fin) EMPEZAR:
            mitad = inicio + (fin - inicio) / 2
                SI ( == Arreglo[i]) ENTONCES:
                    REGRESA mitad
                SINO
                    fin = mitad - 1
        TERMINAR
    TERMINAR METODO

4 SALIDA
4.1 METODO table():
        int n = Arreglo.lenght()
        int mitad = n/2
        MS = TElapser(Arreglo[], 0)
        PS = TElapser(Arreglo[], Arreglo[mitad])
        PRS = TElapserS(Arreglo[], -1)
        MB = TElapserB(Arreglo[], Arreglo[mitad])
        PB = TElapserB(Arreglo[], 2)
        PRB = TElapserB(Arreglo[], -1)

        ESCRIBE("COMPARACION MEJOR, PEOR Y PROMEDIO")
        ESCRIBE("METODO DE BUSQUEDA  | MEJOR | PEOR | PROMEDIO")
        ESCRIBE("BUSQUEDA SECUENCIAL | MS    | PRS  | PS")
        ESCRIBE("BUSQUEDA SECUENCIAL | MB    | PRB   | PB")
    TERMINAR METODO

4.2 METDO long TElapserS(int[] Arreglo, int val):
        TimeElapsed crono = new TimeElapsed()
        searchSecuencial(Arreglo[], val)
        crono.calcula()
        REGRESA crono.te
    TERMINAR METODO

4.2 METODO long TElapserB(int[] Arreglo, int val):
        TimeElapsed crono = new TimeElapsed()
        serchBinaria(Arreglo[], val)
        crono.calcula()
        REGRESA crono.te
    TERMINAR METODO

5 NAVEGACION
5.1 METODO navigation():
        goal()
        data()
        tabla()
    TERMINAR METODO

METODO main():
    navigation()







